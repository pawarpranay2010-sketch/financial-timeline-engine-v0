# Stage 2 web backend
#
# Split manifests (preferred):
#   requirements-core.txt — hosted API + deterministic runtime (mobile-safe,
#                           ~49 MiB, no pandas/numpy/streamlit/redis)
#   requirements-ui.txt   — Streamlit frontend + pandas (needs wheel support)
# This full manifest remains for desktop/Railway installs.

fastapi>=0.115
uvicorn[standard]>=0.30

# Streamlit (legacy frontend — still present)
streamlit>=1.32

requests>=2.31
gradio_client>=1.0
pandas>=2.3.3
python-docx>=1.1
pypdf>=4.0
reportlab>=4.0
openpyxl>=3.1

google-genai>=0.3

SQLAlchemy>=2.0
psycopg2-binary>=2.9

redis>=5.0

pydantic>=2.0
pydantic-settings>=2.0

pyyaml>=6.0

python-dotenv>=1.0

apscheduler>=3.10

tenacity>=8.2

numpy>=1.26

pytest>=8.0
