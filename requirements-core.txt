# Platrixa — CORE manifest: hosted API + deterministic runtime only.
#
# Scope (evidence-based, Phase 18 follow-up):
#   `import api.main` (full stack: FastAPI boundary, Kernel, semantic IR,
#   metering gate, evidence store, rules) imports NONE of
#   pandas/numpy/streamlit/redis and peaks at ~49 MiB RSS.
#   This manifest is the dependency set that path actually needs —
#   suitable for Termux / free containers where pandas/numpy wheels would
#   require an on-device C toolchain.
#
# Install:   pip install -r requirements-core.txt
# Full app:  pip install -r requirements-core.txt -r requirements-ui.txt
#            (or the unchanged requirements.txt on desktop/Railway)
#
# Runtime (measured third-party import set of api.main + kernel path):
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
python-multipart>=0.0.9
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
pyyaml>=6.0
requests>=2.31
gradio_client>=1.0
groq>=1.7.0

# Developer-facing regression suites (fte_*) use TestClient + embedded PG:
httpx>=0.27
pgserver>=0.1 ; sys_platform != "win32"
